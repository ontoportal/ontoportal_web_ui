- return "" if (@submission.respond_to?(:status) && @submission.status == 404) || @ontology.summaryOnly
#metrics_content{:style => "margin-left: 1em;"}
  %div{:style => "clear:both;"}
  %div{:style => "width: 400px;"}
    %h2
      %span{:style => "font-size: x-large;"} Metrics
      = help_icon("http://www.bioontology.org/wiki/index.php/Ontology_Metrics", { :title => "View individual metrics definitions", :style => "margin-top: -7px;" })
  /
    <p style="border: 1px solid rgb(239, 239, 239); margin: 10px; padding: 10px;">
    <haml:loud>t('ontologies.metrics.intro')</haml:loud>
    </p>
  - if @metrics.nil? || @metrics.empty?
    %h2{:style => "padding-left: 1em;"} We have not yet calculated metrics for this ontology.
  - else
    - if @old_metrics
      %h3
        Metrics are not available for the most recent version of this ontology. The metrics displayed below are for the
        %a{:href => "/visualize/#{@old_metrics.id}"}= @old_ontology.displayLabel + " " + @old_ontology.versionNumber.to_s
        version.
    :css
      .metrics_table td {
        text-align: right;
      }
    %table.form.metrics_table{:style => "width: 400px;"}
      /
        not calculated currently
        <tr>
        <th>Number of axioms:</th><td class="top"><haml:loud>@metrics.numberOfAxioms</haml:loud></td>
        </tr>
      %tr
        %th Number of classes:
        %td.top= @metrics.classes  # @metrics.numberOfClasses
      %tr
        %th Number of individuals:
        %td= @metrics.individuals  # @metrics.numberOfIndividuals
      %tr
        %th Number of properties:
        %td= @metrics.properties  # @metrics.numberOfProperties
      %tr
        %th Maximum depth:
        %td= @metrics.max_depth  # @metrics.maximumDepth
      %tr
        %th Maximum number of children:
        %td= @metrics.max_children  # @metrics.maximumNumberOfSiblings
      %tr
        %th Average number of children:
        %td= @metrics.avg_children  # @metrics.averageNumberOfSiblings
      %tr
        %th{:style => "vertical-align: top;"} Classes with a single subclass:
        %td
          = @metrics.classes_one_child
          =# format_metric_list(@metrics, @metrics.classes_one_child, "Classes with a single subclass")
      %tr
        %th{:style => "vertical-align: top;"} Classes with more than 25 subclasses:
        %td
          = @metrics.classes_25_children
          =# format_metric_list(@metrics, @metrics.classes_25_children, "Classes with more than 25 subclasses")
      %tr
        %th{:style => "vertical-align: top;"} Classes with no definition:
        %td
          = @metrics.classes_with_no_definition
          =# format_metric_list(@metrics, @metrics.classes_with_no_definition, "Classes with no definition")
      /
        <tr>
        <th style="vertical-align: top;">Classes with no author:</th>
        <td>
        <haml:loud> format_metric_list(@metrics, @metrics.classesWithNoAuthor, &amp;quot;Classes with no author&amp;quot;) </haml:loud>
        </td>
        </tr>
      /
        not calculated currently
        <tr>
        <th style="vertical-align: top;">Classes with more than one property value:</th>
        <td>
        <haml:loud> format_metric_list(@metrics, @metrics.classesWithMoreThanOnePropertyValue, &amp;quot;Classes with more than one property value&amp;quot;) </haml:loud>
        </td>
        </tr>
