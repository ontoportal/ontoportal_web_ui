- content_for :viewer_content do
  .row.g-3.pb-4
    .col-lg-8
      / === Latest Submission Log ===
      .card{ data: { controller: 'user-ontology-log', 'user-ontology-log-url-value': submission_log_ontology_path(@ontology.acronym) } }
        .card-header.d-flex.align-items-center.justify-content-between
          %span Latest submission log
          .d-flex.gap-2
            = link_to '#', class: 'btn btn-sm btn-outline-secondary', data: { action: 'user-ontology-log#refresh' } do
              Refresh
            = link_to submission_log_ontology_path(@ontology.acronym),
                      class: 'btn btn-sm btn-outline-secondary',
                      data: { turbo: false } do
              Download Full Log
        .card-body.overflow-auto{ style: "max-height: 300px;" }
          %pre.mb-0.font-monospace{ data: { 'user-ontology-log-target': 'content' } }
            Loading…

    .col-lg-4
      / === Danger Zone ===
      .card.border-danger
        .card-header.text-danger Danger zone
        .card-body
          %p.small.text-muted
            Deleting the ontology removes submissions, notes, mappings and related data. This cannot be undone.
          .mb-2
            %label.form-label{ for: "confirm_acronym" } Type the acronym to confirm:
            %input#confirm_acronym.form-control.form-control-sm{ placeholder: "EPO" }
          %button.btn.btn-outline-danger.btn-sm{ disabled: true } Delete ontology “EPO”

      / === Quick actions ===
      .card.mt-3
        .card-header Actions
        .card-body
          %a.btn.btn-primary.btn-sm.me-2{ href: "#" } New submission
          %a.btn.btn-outline-secondary.btn-sm{ href: "#" } Edit metadata