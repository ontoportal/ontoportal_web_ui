</div>
</div>

%footer.footer.pt-4.mt-5
  %div.container
    %div.row
      - $FOOTER_LINKS[:top].to_a.each do |footer_links_block|
        %div.col-6.col-md
          %h6= footer_links_block[:column_header]
          %ul.list-unstyled
            - footer_links_block[:links].each do |footer_links_row|
              %li
                - footer_links_row.each do |footer_link|
                  = link_to(footer_link[:url], target: footer_link[:target], style: 'text-decoration: none') do
                    - if footer_link[:text].present?
                      = raw(footer_link[:text])
                    - else
                      - if footer_link[:img_src].present?
                        = image_tag footer_link[:img_src]
                      - else
                        - if footer_link[:icon].present?
                          = content_tag(:i, '', class: footer_link[:icon])
    %div.row.legal-text
      %div.col
        = t('.grant_html', site: $SITE)
    %div.row.pt-2
      - $FOOTER_LINKS[:bottom].to_a.each do |footer_links_block|
        %div.col-6.col-md
          - if footer_links_block[:column_header].present?
            %h6= footer_links_block[:column_header]
          %ul.list-inline
            - footer_links_block[:links].each do |footer_links_row|
              - footer_links_row.each_with_index do |footer_link, i|
                %li.list-inline-item
                  = link_to(footer_link[:url], target: footer_link[:target]) do
                    - if footer_link[:text].present?
                      = raw(footer_link[:text])
                    - else
                      - if footer_link[:img_src].present?
                        = image_tag footer_link[:img_src]
                      - else
                        - if footer_link[:icon].present?
                          = content_tag(:i, '', class: footer_link[:icon])
                - unless i >= footer_links_row.size - 1
                  %li.list-inline-item
                    = raw(footer_links_block[:separator])

= javascript_include_tag "application"
= render partial: "ga_tracking"
