= turbo_frame_tag 'application_modal_content' do
  = form_with url: agent_path(@agent.id.split('/').last), method: 'post', data:{turbo: true} do
    = render partial: 'form', locals: {agent: @agent, name_prefix: '', new_agent: true}
